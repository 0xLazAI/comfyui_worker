# Hand-written GraphQL operation documents.
#
# Put bespoke queries/mutations here — custom selection sets, multi-field
# documents, or anything you do not want the auto-generator to manage.
# codegen picks these up alongside operations/generated/**.
# This folder is never touched by npm run graphql:operations.
